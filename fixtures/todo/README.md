# TODO ownership fixtures

The `QC.TODO.OWNER` adapter scans tracked text in a clean Git `HEAD`. The positive fixture uses
the accepted bounded marker format; the negative fixture contains an unowned marker. Copy either
directory into a disposable Git checkout before invoking the adapter. No fixture contains product
code or a real ticket.
