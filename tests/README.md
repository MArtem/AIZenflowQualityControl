# Verifier Tests

This directory is reserved for future unit, integration, fixture, and adversarial tests of the
quality-control engine.

No tests are created, modified, or executed in Stage 4. Adding tests requires a separately approved
test-writing phase and must demonstrate that broken, stale, skipped, denied, bypassed, or forgeable
evidence cannot produce normal `PASS`.
