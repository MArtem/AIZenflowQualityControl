# Manual Workflows

This directory is reserved for future manually triggered, advisory GitHub workflow definitions.

No workflow exists in Stage 4. Future workflows must use pinned dependencies, least privilege,
bounded artifacts, exact-source evidence, standard included runners, zero additional monetary cost,
and no paid AI API calls. They must not make branch protection or CI mandatory without a separate
user decision.
